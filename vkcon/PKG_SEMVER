# version string expected in following format
# Major.Minor.Patch
# where Major, Minor and Patch are numeric characters
# Used to control semantic versioning of each package
# This version is passed to libtool also.
# use up to 3 digits for each numeric value (due to libtool)
0.2.0
